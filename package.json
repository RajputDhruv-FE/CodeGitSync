{
  "name": "leetcode-github-sync",
  "version": "1.0.0",
  "description": "Automatically detect accepted LeetCode submissions",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "typescript": "^5.0.0"
  }
}
